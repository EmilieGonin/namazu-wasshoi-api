const { confirm } = require('./functions');

module.exports = main = {
  // destroyall: (msg) => {
  //   confirm(msg, '!shoi destroyall').then(confirmed => {
  //     if (confirmed) {
  //       msg.channel.bulkDelete(100);
  //     } else {
  //       msg.delete();
  //     }
  //   })
  // },
  test: () => {
    console.log('test');
  }
}
